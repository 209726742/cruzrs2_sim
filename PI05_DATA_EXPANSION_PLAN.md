# π0.5 棒状物搬运任务：第一性原理数据扩充方案

> 状态：执行中；v2 完整轨迹 16/16 通过，混合数据集审计与 5-step 预检通过，正在运行 3k canary
> 日期：2026-08-28
> 适用范围：当前 CRUZR MuJoCo `sorting_roll_v15` 三相机、18 维状态/动作、30 Hz 的 π0.5 行为克隆任务

## 当前执行状态

- 旧 v1 pilot 的 16/16 源数据和格式审计虽通过，但 T/R 是使用完整任务指令的中途终止轨迹；该数据集不再作为扩量依据。
- 匹配 3k canary 的固定 12 场景结果为：original/control/treatment 严格双手抓持 7/8/11，稳定抬升 0/0/1，端到端成功均为 0，故旧 treatment 未通过绝对门槛。
- teacher-forced 证实旧 treatment 在抬升起点的双臂 20 步 MAE 为 0.089/0.111 rad、方向余弦 0.607；original/control 仅约 0.006–0.008 rad、方向余弦 0.998。
- 退化从 500 step 已出现，不是训练过久：旧 T 中 63.9% 是接近桌面、抬升仅 11.1%；旧 R 中 55.3% 是 `clear_table`，且 π0.5 未屏蔽末端 padding。
- v2 契约要求 H/T/R 从各自起点录制后继续到 `terminal_success_hold`，不再以抬升或离桌作为完整任务终点。
- v2 完整 pilot 已 16/16 成功、0 失败，H/T/R=4/4/8；全部录制到 `terminal_success_hold`，1092–1560 帧、50.6–54.6 秒。
- 新混合 LeRobot v3.0 数据集有 256 回合、387,376 帧，train/val/test=252/2/2；三路 RGB 均为 224×224@30 FPS，state/action 均为 18 维 float32，24 个抽样视频片段可解码，数据审计 `passed=true`。
- canary 使用保守逐帧采样 old/H/T/R=70/10/10/10；采样权重精确达到目标质量，数据 readiness 为 `true`。
- 完整回归测试 250/250 通过；2×4090 的 5-step 实际训练预检完成，退出码为 0。
- 当前 tmux `sorting_roll_v16_full_v2_treatment_expert3k` 正从 36k 权重起点运行 3,000 步 treatment canary；不恢复旧 optimizer、scheduler 或 dataloader，保留旧 normalization stats。
- canary 后仍必须通过 teacher-forced 抬升诊断和同一 fixed12 闭环绝对门槛；未通过前不得扩到 80/160 回合或重新开始正式训练。
- 退出条件不变：修正版 treatment 必须在同一固定闭环套件中优于匹配对照且通过绝对门槛，否则停止该类扩充并重新检查监督设计。

## 0. 结论先行

当前最值得做的事情不是继续扩大均匀随机数据，也不是继续尝试更多 `REPLAN` 数值，而是补齐模型真正缺失的状态—动作关系：

1. 抓取前的精确对准；
2. 双手同时建立接触；
3. 抓住后稳定承重并抬离支架；
4. 抓偏、单手接触、滑落、碰移之后的恢复；
5. 搬运与转向过程中的持续持物；
6. 货架偏移后的重新对准、插入和释放；
7. 多目标场景中由语言决定抓哪一根棒子。

建议新增 **160 条被验收通过的 episode**，而不是再录 300 条同分布完整成功轨迹：

| 数据族 | 总数 | train | val | test | 主要作用 |
|---|---:|---:|---:|---:|---|
| H：困难条件下的完整成功轨迹 | 40 | 32 | 4 | 4 | 保留完整任务链并覆盖关键交互项 |
| T：阶段聚焦的正确轨迹 | 40 | 32 | 4 | 4 | 提高抓取、抬升、持物和释放的有效帧占比 |
| R：失败状态后的恢复轨迹 | 56 | 48 | 4 | 4 | 学会失败后重定位、重抓、放回再抓和重新插入 |
| C：多物体颜色—语言反事实轨迹 | 24 | 16 | 4 | 4 | 让颜色词真正决定目标，而不是成为无意义标签 |
| **合计** | **160** | **128** | **16** | **16** | 当前 240 条训练 episode 扩充为 368 条 |

在采集代码、恢复专家和多物体场景已经实现并通过 canary 的前提下，按现有真实吞吐保守估算：

- **8×RTX 4090：约 1.6–2.0 小时完成采集、重试、验证、构建和审计；实际排期建议留 2–2.5 小时。**
- **4×RTX 4090：约 2.7–3.2 小时完成同样流程；实际排期建议留 3–4 小时。**

这些时间不包含一次性的场景、恢复专家、验证器和多物体任务开发。该开发不是小改动，合理预算是 **1.5–3 个工程日**。如果恢复数据依靠单人手工遥操作而不是自动专家，GPU 数量不会按比例提速，56 条恢复轨迹本身预计需要约 **5–8 小时操作员时间**，还要另算返工与复核。

---

## 1. 第一性原理拆解

### 1.1 已经确认、无法绕开的基本事实

#### 事实 A：当前数据量是 300 条 episode，但真正进入训练的是 240 条

当前正式数据集有：

- train：240 条；
- val：30 条；
- test：30 条；
- 总帧数：458,786；
- 频率：30 Hz；
- 对应约 15,293 秒、4.25 小时仿真轨迹。

原始 300 次自动专家采集中成功 293 次，初始成功率为 97.67%。7 条失败轨迹后来均被成功候选替换，因此最终训练数据是 **全成功数据**。这保证了标签正确，却也把真实策略最需要学习的失败状态和恢复动作全部排除掉了。

证据来自当前 campaign 的报告与实现：

- `cruzr_mujoco_sim/output/sorting_roll_expert/sorting_roll_v15_diverse300_20260826_8x4090/initial_collection_report.json`
- `cruzr_mujoco_sim/output/sorting_roll_expert/sorting_roll_v15_diverse300_20260826_8x4090/replacement_collection_report.json`
- `cruzr_mujoco_sim/scripts/collection/sorting_roll_v15_finalize.py`

#### 事实 B：现有边际多样性不错，但关键联合条件非常稀疏

当前 300 条数据的边际分布为：

- 物体尺寸：3 档，各 100 条；
- 位姿难度：easy 120、medium 120、boundary 60；
- 动力学：nominal 180、light/high-friction 60、heavy/low-friction 60；
- 颜色：红、橙、黄、绿、蓝，各 60；
- 光照：normal 180、dim 60、bright 60；
- JPEG：clean 180、mild 60、strong 60；
- 指令模板：5 种，各 60。

但在 240 条训练 episode 中，“物体尺寸 × 动力学 × 位姿”共有 27 个组合格：

- 最少的格只有 1 条；
- 中位数只有 7 条；
- 最多 25 条；
- `long_baseline × heavy_low_friction × boundary` 只有 5 条。

这说明“每个维度分别均衡”不等于“最影响成功率的交互项被覆盖”。抓取难度不是长度、摩擦、质量、位姿四个独立问题，而是它们共同决定可容忍的对准误差、夹持稳定性和抬升加速度。

当前分配逻辑见 `cruzr_mujoco_sim/scripts/core/sorting_roll_diversity.py`。

#### 事实 C：抓取和抬升只占现有帧的 4.58%

对 300 条最终入选轨迹的 `phase` 逐帧统计：

| 阶段 | 总帧占比 |
|---|---:|
| 导航到桌前观察位 | 15.86% |
| 靠近桌边 | 8.93% |
| 水平接近并抓取 | 3.00% |
| 抬离支架 | 1.58% |
| 清桌后退 | 7.65% |
| 转向货架 | 9.23% |
| 导航到货架 | 10.82% |
| 越过货架前唇 | 11.21% |
| 受控释放 | 8.62% |

模型当前反复失败的“接触 → 承重 → 抬升”只有约 4.58% 的帧。继续添加同长度完整轨迹，会同时添加大量已经相对会做的导航和静态等待帧，单位采集成本带来的有效抓取监督很低。

#### 事实 D：当前初始位姿随机化范围很窄，且没有支架、货架和高度随机化

当前代码的范围是：

- 底盘 `x/y`：±15 mm；
- 底盘 yaw：±0.025 rad，约 ±1.43°；
- 棒子 `x/y`：±4 mm；
- 棒子 yaw：±0.012 rad，约 ±0.69°。

当前没有随机化：

- 棒子初始高度；
- 两个拾取支架的共同位置、高度差或姿态；
- 桌子整体位置；
- 货架/槽位整体位置和姿态；
- 相机外参；
- 抓取执行过程中的可控扰动。

代码位置：`cruzr_mujoco_sim/scripts/collection/sorting_roll_expert.py` 中的 `seed_randomization()` 和 `apply_scene_randomization()`。

#### 事实 E：当前观测里没有力或触觉反馈

π0.5 当前看到的是：

- 三路 RGB：头部左目、左腕、右腕；
- 14 个手臂关节位置；
- 左右夹爪开合比例；
- 底盘前向速度和角速度。

它看不到：

- 左右手接触力；
- 哪些指垫正在接触；
- 物体质量和摩擦系数；
- 显式的“是否抓住”标志；
- 显式的全局底盘 `x/y/yaw`。

因此，物理外观相同但质量或摩擦不同的棒子，在接触前对策略来说不可区分。仅靠增加数据，无法让模型在接触前准确选择“对应的抓取力度”。可行路径只能是：

1. 让低层夹爪控制器在目标开合命令下自动形成稳定、受限的力；或
2. 如果真机也有可靠传感器，把力/触觉加入模型观测；或
3. 明确通过指令给出“重棒/轻棒”等可观测任务条件，但接触后的闭环仍最好依赖传感器。

不能为了仿真效果加入真机没有的力特征，否则会制造新的 sim-to-real 缺口。

#### 事实 F：成功专家的双手力很集中，失败策略的力明显异常

300 条成功专家轨迹的接触力分布：

| 阶段 | 左手 P05 / 中位 / P95 | 右手 P05 / 中位 / P95 | 双手力比 P95 |
|---|---|---|---:|
| 首次抓住 | 18.63 / 19.82 / 22.25 N | 19.55 / 20.75 / 22.49 N | 1.10 |
| 抬离支架 | 18.37 / 19.49 / 19.79 N | 19.31 / 20.56 / 20.86 N | 1.08 |
| 搬运到货架 | 18.29 / 19.37 / 19.54 N | 19.11 / 20.21 / 20.40 N | 1.05 |

相比之下，失败 rollout 出现过 36–135 N 的峰值和明显左右不对称。这不只是“抓紧一点”或“抓松一点”，而是接触位置、同步性、夹爪指令和抬升轨迹共同失配。

#### 事实 G：checkpoint 变新没有带来单调改进

固定 `seed=3000 / policy seed=28000 / REPLAN=20` 的闭环结果：

| checkpoint | 严格双手抓持 | 最大抬升 | 结果 |
|---|---:|---:|---|
| 28k | 13.57 s | 6.76 mm | 抓住但无法持续承重 |
| 31k | 无 | 0.0003 mm | 停在接近/对准循环 |
| 36k | 12.90 s | 16.78 mm | 短暂抬起后回到桌面 |
| 40k | 无 | 0.80 mm | 导航过度旋转，未形成抓取 |

`REPLAN=30` 能让动作执行到更深的 chunk 尾段，但 28k 和 31k 都出现强推、倾斜或掉落。由此可以确认：

- 训练 loss 低不等于闭环成功；
- 更多训练步数不保证更好；
- 重规划窗口会改变失败形态，但不是根因；
- 必须按闭环阶段指标选 checkpoint。

#### 事实 H：当前“颜色多样性”不是颜色语言 grounding

当前场景只有一根棒子，5 个 prompt 都没有颜色词。即使把棒子渲染成红、黄、蓝等颜色，机器人始终只有一个可抓目标，因此模型完全可以忽略颜色仍然获得正确动作。

要让“抓红色棒子”和“抓黄色棒子”具有不同含义，同一画面中必须至少出现两个候选棒子，并且只改变指令就会改变正确动作。否则颜色只是视觉增强，不是语言理解。

如果语音先经过 ASR 再把文本传给 π0.5，那么策略数据只能训练文本 grounding，不能训练口音、噪声和识别错误；ASR 鲁棒性属于另一条数据链路。

### 1.2 习惯性接受、却没有验证过的假设

下列假设不能继续默认成立：

1. **“继续训练到更多 step 会自然学会抓取。”** 31k、40k 相对 36k 的回退已经否定单调性。
2. **“每个随机维度分别均衡，就拥有组合泛化。”** 联合格只有 1–25 条，困难格仅 5 条。
3. **“全成功示范足以让策略在失败后恢复。”** 行为克隆只会模仿见过的状态；失败状态被全部替换掉了。
4. **“增加随机颜色并改 prompt 就能学会颜色。”** 单目标场景下颜色不影响动作，没有可识别的因果关系。
5. **“抓力问题可以完全靠 RGB 数据解决。”** 当前观测没有力/触觉，质量和摩擦在接触前不可见。
6. **“把随机范围放大总是有益。”** 超出专家可达域或真实部署域的样本会产生冲突标签和无效长尾。
7. **“每条 episode 的信息价值相同。”** 当前 4.58% 的抓取/抬升帧说明完整轨迹严重偏向导航。
8. **“一个 seed 的成功或失败可以代表 checkpoint。”** π0.5 采样具有随机性，场景也有多因素变化；单 seed 只能做 canary。
9. **“当前问题一定只有数据原因。”** 官方推理和输入输出契约已严格验证，但尚未完成专家观测下的 teacher-forced 动作诊断，不能排除优化、chunk 前缀或闭环状态漂移的贡献。
10. **“代码里存在恢复函数，就代表训练数据有恢复。”** 当前 `flatten_hands()` 等再抓取逻辑没有进入主轨迹，最终数据中没有对应 phase。

### 1.3 真正想实现的目标

目标不是让某个 checkpoint 在 seed3000 上偶然抬高几毫米，而是让策略在明确定义的运行域内：

1. 找到正确目标并到达可抓工作区；
2. 两手在合理位置、合理同步误差内形成接触；
3. 以稳定力平衡抬升至少 70 mm，并持续至少 1 秒；
4. 搬运、后退和转向期间不滑落；
5. 对准真实可见的货架并安全插入；
6. 释放后棒子在顶层槽位稳定至少 2 秒；
7. 出现可恢复错误时主动重定位和重试；
8. 多目标场景中服从颜色语言，且不明显移动干扰物。

建议把最终成功目标拆成阶段指标：

| 指标 | 建议目标 |
|---|---:|
| 导航到可抓工作区 | ≥95% |
| 严格双手抓持 | ≥90% |
| 稳定抬升 ≥70 mm、持续 ≥1 s | ≥85% |
| 搬运后仍保持 | ≥85% |
| 货架插入与释放 | 在到达该阶段的样本中 ≥90% |
| nominal 端到端成功率 | ≥90% |
| hard 端到端成功率 | ≥75%，之后再提升到 ≥85% |
| 可恢复扰动恢复成功率 | 第一阶段 ≥60%，成熟目标 ≥80% |
| 多目标颜色选择正确率 | ≥90% |
| 干扰棒移动超过 20 mm | ≤5% |

这些数字是建议的工程门槛，不是物理定律；在正式采集前应由部署方确认可接受的运行域和风险。

### 1.4 现实中的资源与约束

1. 可用采集资源是 4×4090 或 8×4090，每卡当前适合运行一个渲染采集 worker。
2. 现有自动专家完整 episode 中位仿真时长约 51.1 秒，但真实每卡墙钟约 2.8 分钟/条，主要成本来自三相机渲染、JPEG、视频编码和文件 I/O。
3. 当前训练入口、审计器和 v15 pipeline 硬编码 300 条、5 个 task 和旧 task version；新增阶段轨迹、多物体和恢复元数据必须使用新版本契约，不能直接伪装成 v15。
4. π0.5 action chunk 为 50 步，即 30 Hz 下约 1.67 秒。关键恢复动作必须在失败状态后的前 20–50 步就有明确方向，否则短窗口重规划仍会反复停在动作前缀。
5. 标准行为克隆只学习“应该做什么”，不会自动理解“这个坏动作不要做”。不能把策略造成失败的错误动作当成专家标签混入训练。
6. 数据预算有限，所以不做所有因素的笛卡尔积；使用风险优先、成对反事实、交互项定额和分阶段停止规则。
7. 如果真实机器人没有力传感器，就不能依赖仿真独有力信息作为策略输入；仿真力仍可用于数据验收和低层控制。

---

## 2. 从任务状态转换重新推导数据需求

### 2.1 任务不是一个动作，而是一条必须闭合的状态链

将任务拆成：

```text
S0 看见场景
  → S1 到达桌前观察位
  → S2 到达可抓工作区并完成预抓对准
  → S3 双手形成有效接触
  → S4 承重并抬离支架
  → S5 持物后退、转向、移动
  → S6 对准货架、插入、释放
  → S7 验证稳定并撤手
```

任何一条边失败，策略都需要三种能力：

1. 从当前观测识别自己处在哪个状态；
2. 判断上一条边是否完成；
3. 在未完成时执行恢复，而不是假定成功继续下一阶段。

现有数据几乎只提供 `S0→S1→…→S7` 的单一路径。闭环策略一旦进入 `S3'`（只抓到一边）、`S4'`（抬起后打滑）或 `S6'`（插入姿态不正），就落到训练分布之外。

### 2.2 系统性故障树与对应数据

| 阶段 | 可能问题 | 当前是否充分可观测 | 高价值数据/系统措施 | 验收指标 |
|---|---|---|---|---|
| 场景识别 | 棒子颜色、尺寸、位置变化 | RGB 可见 | 成对场景、尺寸和位姿交互覆盖 | 目标定位与选择正确 |
| 场景识别 | 多棒时抓错颜色 | 单棒场景不可验证 | 同画面双棒，命令互换 | 目标正确，干扰物不动 |
| 导航 | 过早停下、原地旋转、朝向反向 | RGB、底盘速度可见；无全局 pose | 对起始偏差和桌/架偏移录正确收敛与纠偏 | 到达误差、累计旋转、耗时 |
| 预抓 | 基座到位但手臂位置不对 | 三相机和关节可见 | 预抓微轨迹，覆盖左右/前后/高度误差 | 无碰撞、进入工作区 |
| 接触 | 两手都没接触 | 视觉可见，力不可输入 | 从 miss 状态后退、重定位、再接近 | 第二次抓取成功 |
| 接触 | 只有一手接触 | 视觉部分可见，力不可输入 | 单手状态下放回或保持—重抓 | 不强行抬升，最终双手接触 |
| 接触 | 抓取点偏心 | RGB 可见 | 不同轴向抓点和回中心动作 | 抬升后倾角受限 |
| 接触 | 夹得太弱 | 接触前不可观测；接触后可见滑移 | 低层力控制；滑移后放回再抓数据 | 不持续下滑 |
| 接触 | 夹得太强、把棒子推走 | 无显式力反馈 | 限力低层控制；被推移后的重定位数据 | 峰值力与左右比受控 |
| 抬升 | 未离开支架 | 视觉可见 | 接触后立即、同步、低加速度抬升微轨迹 | ≥70 mm 且离开支架 |
| 抬升 | 一边先抬导致倾斜 | RGB 可见 | 左右高度误差、同步纠偏 | 轴向 z 分量、双手高度差 |
| 抬升 | 抬起后掉回桌面 | RGB 可见 | 滑移→降低→放回→重新抓取 | 重抓后持续持物 ≥1 s |
| 搬运 | 后退/转向时滑落 | RGB 可见但无力反馈 | 速度/加速度阶梯和中途恢复 | 转向后保持率 |
| 搬运 | 物体遮挡腕部视野 | 部分可见 | 合理相机外参、姿态和短时遮挡 | 不因短暂遮挡失控 |
| 货架 | 货架位置/高度偏移 | 若在视野内则可见 | 货架共同平移/偏航/高度变化 | 重新对准而非走固定坐标 |
| 插入 | 棒子轴不平、撞前唇 | RGB 可见 | 碰撞前后退、重新调平、再插入 | 无持续碰撞，轴误差受控 |
| 释放 | 过早释放、棒子掉落 | RGB 可见 | 恢复插入深度后再释放 | 释放后稳定 ≥2 s |
| 释放 | 夹爪没完全松开 | 开合比例可见 | 延迟释放、撤手前确认 | 双手无接触后撤离 |
| 安全边界 | 棒子掉出桌面或超出可达域 | 可见但可能不可恢复 | 不混入同任务恢复训练；进入安全停止/单独任务 | 不产生危险追逐动作 |
| 传感 | 相机轻微偏移、曝光变化、压缩 | RGB 可见 | 物理一致的外参/光照/成像扰动 | 不显著降低阶段成功率 |
| 传感 | 丢帧、状态与图像错位 | 当前契约要求同步 | 优先修数据管线；不要把错误同步当“多样性” | skew ≤20 ms、帧连续 |

### 2.3 哪些问题可以靠数据解决，哪些不能

#### 可以主要靠数据解决

- 可见的棒子、支架、桌子、货架位置和姿态偏移；
- 不同尺寸、颜色、光照、成像质量；
- 抓偏后重新对准；
- 棒子在桌面上被推移后的重定位；
- 抬起后可见滑移时的放回与重抓；
- 货架插入碰撞后的后退和再对准；
- 多物体场景中的颜色目标选择。

#### 不能只靠增加相同观测的数据解决

- 外观完全相同但质量/摩擦不同，且观测中没有力/触觉或任务提示；
- 夹爪输出没有限力能力，却要求策略精确控制不可观测的接触力；
- 棒子或货架已完全离开所有相机视野；
- 标签时间错位、相机状态不同步、坐标变换错误；
- 同一观测和同一 prompt 被赋予互相矛盾的恢复动作；
- 真机不可实现、只在仿真中存在的恢复动作。

---

## 3. 信息效率最高的 160 条数据设计

### 3.1 H 族：40 条困难条件下的完整成功轨迹

这些 episode 从初始位置执行到顶层稳定放置，作用是保持全任务时序，并让困难因素在完整闭环中共同出现。

| 子类 | 条数 | 设计 |
|---|---:|---|
| H1 长棒×重低摩擦×边界位姿 | 16 | 直接把当前仅 5 条训练样本的关键格提升到可学习规模；交叉支架高度、左右偏移和两档光照 |
| H2 拾取支架与初始高度 | 8 | 整套拾取机构共同平移/偏航/升降；棒子稳定落在支架上后开始 |
| H3 货架/槽位偏移 | 8 | 货架碰撞、视觉、目标和成功判定一起变换；覆盖 x/y/z 和 yaw 的角点 |
| H4 相机与成像扰动 | 8 | 小幅外参、曝光/光照、JPEG 组合；不得破坏相机同步和真实部署合理性 |

设计原则：

- 不采用完全独立随机；使用小型 pairwise/coverage-array 设计，让每个高风险因素至少与另一个因素共同出现。
- 至少一半 H1 样本使用非默认支架高度或相对位姿。
- 所有 H 族都必须端到端成功，失败执行可以保留作调试和未来场景挖掘，但不能直接作为正向行为克隆标签。

### 3.2 T 族：40 条阶段聚焦的正确轨迹

阶段轨迹不是从远处完整导航开始，而是从经过验证、物理可达的中间状态开始。这样可以在不增加大量 episode 的情况下，显著提高关键阶段帧占比。

| 子类 | 条数 | 起点与终点 | 关键变化 |
|---|---:|---|---|
| T1 预抓与双手接触 | 12 | 桌前观察/预抓 → 严格双手接触 | 抓点轴向±、左右同步、棒子横向/偏航、不同直径 |
| T2 承重与稳定抬升 | 16 | 接触前约1秒 → 抬升85 mm并保持1–2秒 | 低/中抬升速度、两手高度微差、长重低摩擦优先 |
| T3 持物后退与转向 | 6 | 已稳定抬升 → 清桌并完成转向 | 速度/加速度阶梯、轻微物体倾角 |
| T4 插入与释放 | 6 | 货架前已持物 → 稳定释放并撤手 | 货架偏移、轴向误差、插入深度和释放时机 |

阶段 episode 建议长度为 5–20 秒，并满足：

- 至少 150 帧；
- 关键状态转换之后仍有至少 50 个未来 action，避免 50 步 chunk 的边界 padding 主导标签；
- 静止等待帧不超过该 episode 的 10%；
- 保存 `start_phase`、`terminal_phase` 和关键事件帧；
- 任务 prompt 与完整任务一致，但元数据必须明确这是阶段轨迹，不能冒充完整端到端成功率样本。

### 3.3 R 族：56 条失败状态后的恢复轨迹

共 7 类，每类 8 条。每条应从可复现的失败状态开始，以正确恢复并最终回到有效任务链为目标。

| 恢复类型 | 条数 | 初始失败状态 | 正确示范 |
|---|---:|---|---|
| R1 导航停滞/过度旋转 | 8 | 桌前距离或朝向偏差，策略趋向停住/旋转 | 停止旋转 → 重新观察 → 收敛到桌前抓取位 |
| R2 双手都没接触 | 8 | 夹爪闭合但没有有效接触 | 张开 → 后退少量 → 重定位 → 再接近 |
| R3 单手或不对称接触 | 8 | 一侧两指垫接触，另一侧未接触或明显偏心 | 不强行抬升；放回/保持安全支撑 → 另一侧重新定位 → 双手重抓 |
| R4 棒子被推移或滚动 | 8 | 棒子仍在桌面，但位置/yaw 已改变 | 撤手 → 重新观察当前位置 → 重新计算抓点 |
| R5 部分抬升后滑落 | 8 | 抬高5–40 mm后开始下滑、倾斜或落回支架 | 降低加速度或安全放回 → 重抓 → 稳定抬升 |
| R6 搬运/转向中倾斜或松动 | 8 | 已离桌，但持物姿态或双手平衡恶化 | 减速/停车 → 调平或放回可支撑面 → 恢复持物 |
| R7 货架插入或释放失败 | 8 | 轴不平、碰前唇、深度不足或过早释放前兆 | 后退 → 调平/居中 → 再插入 → 受控释放 |

#### 恢复数据最重要的标签规则

行为克隆不能把“导致失败的动作”自动理解为负例。因此：

- 优先用仿真外部扰动直接构造失败状态，然后从扰动结束后的正确专家动作开始记录；
- 或让当前策略运行到失败状态，再切换专家，但训练片段必须裁掉接管前的错误策略动作；
- 可以保留错误前缀用于分析和评测，不能把它当成期望 action；
- 恢复开始后的前 20 步就应表现出明确纠错意图，不能先长时间静止；
- 每条恢复轨迹必须记录 `intervention_type`、扰动量、接管帧和恢复完成帧；
- 对物理不可恢复或超出工作空间的状态，应定义安全停止或独立安全任务，不能在相同 prompt 下随意混入“继续追”和“停止”两种矛盾标签。

### 3.4 C 族：24 条多物体颜色—语言反事实轨迹

推荐建立 12 个双棒场景，每个场景采两条：场景、初始状态和动力学完全相同，只交换语言目标。

示例：

```text
场景不变：左侧红棒，右侧黄棒
episode A：抓取红色棒子并放到顶层槽位
episode B：抓取黄色棒子并放到顶层槽位
```

24 条可按以下方式设计：

- 红、橙、黄、绿、蓝共有 10 个无序颜色对，每对至少一个场景，两条目标命令，共 20 条；
- 再选择 2 个高混淆颜色对，交换左右位置，各录两条，共 4 条；
- 训练/验证/测试按 `counterfactual_pair_id` 整组划分，绝不能把同一场景的一条放训练、另一条放测试；
- 每种颜色都要在训练集中既出现在左边也出现在右边，既当目标也当干扰物；
- 两根棒子的尺寸和动力学在第一轮保持相同，先隔离验证颜色 grounding，确认后再引入颜色×尺寸交互；
- 成功判定必须绑定目标 object ID；同时检查干扰棒位移不超过 20 mm。

建议 prompt 使用部署时真实会出现的语言。如果部署主要使用中文，先以中文为主，例如：

- “抓取红色棒子并放到顶层槽位”；
- “把黄色圆棒拿起来，稳定放到最上层”；
- “请将蓝色棒子放进顶部支架”。

同义模板不能与固定颜色、固定左右位置或固定难度绑定。若要同时支持中英文，应把 `task_language` 写入元数据，并确保两种语言在目标颜色和位置上均衡；24 条预算下不宜再无限扩展语言风格。

---

## 4. 几何、物理和感知参数应该怎样扩展

### 4.1 先定义运行域，再在运行域内采样

范围不是越大越好。下面是第一轮建议范围；所有 hard 角点必须先通过专家 admission。

| 因素 | 当前范围 | 建议 core | 建议 hard | 注意事项 |
|---|---|---|---|---|
| 底盘初始 x/y | ±15 mm | ±20 mm | ±35 mm | 与桌/架偏移交叉，不只单独变化 |
| 底盘初始 yaw | ±1.43° | ±2° | ±4° | 监控累计旋转，防止40k式发散 |
| 整套拾取机构 x/y | 固定 | ±10 mm | ±20 mm | 两个支架与棒子共同变换 |
| 整套拾取机构 yaw | 固定 | ±1° | ±3° | 目标必须仍在三相机视野和工作区 |
| 支架共同高度 | 固定1.100 m | {-10, 0, +10} mm | {-15, +15} mm | 棒子 z 随支架物理稳定放置，不能悬空 teleport |
| 两支架高度差 | 0 | ±3 mm | ±6 mm | 用来制造小倾角，先验证支撑稳定 |
| 棒子相对支架横向偏移 | 当前约±4 mm世界偏移 | ±4 mm | ±8 mm | 采集前先 settle，检查没有滚落 |
| 棒子相对支架 yaw | ±0.69° | ±2° | ±5° | 与长重低摩擦交叉 |
| 棒子沿轴偏移 | 当前约±4 mm | 按尺寸计算 | 按尺寸计算 | 必须保留支撑端安全余量，不可统一给±15 mm |
| 货架 x/y/z | 固定 | ±10/10/5 mm | ±20/20/10 mm | 视觉、碰撞、目标和评价器必须一起移动 |
| 货架 yaw | 固定 | ±1° | ±2° | 验证仍可达且不会与机器人初始碰撞 |
| 相机平移外参 | 固定 | ±2 mm | ±4 mm | 每个episode内固定，符合真实安装误差 |
| 相机旋转外参 | 固定 | ±0.5° | ±1° | 不破坏SDK相机契约和可见性 |
| 光照/JPEG | 已有3×3档 | 保留 | 与几何困难交叉 | 不需要继续增加更多离散档 |
| 质量/滑动摩擦 | 已有±15%左右 | 保留现有3档 | 优先补联合格 | 不在第一轮继续扩大不可观测动力学范围 |

棒子沿轴方向可偏移多少，必须按物体长度和支架外缘计算。例如当前支架外缘接近 `x=±0.233 m`，短棒半长约 0.2337 m，几乎没有统一轴向偏移余量。如果要对短棒做明显轴向偏移，应先随物体长度调整支架间距，而不是让棒子失去稳定支撑。

### 4.2 高度变化要物理一致

用户提出“最开始放置棒子的高度不一样”是高价值变化，但正确做法不是只改棒子 z：

1. 改变支架共同高度或使用稳定垫块；
2. 根据半径设置棒子初始中心高度；
3. 让物理仿真 settle；
4. 验证两端都受支撑、初速度足够小；
5. 再开始记录。

直接把棒子悬在不同高度，会把自由落体瞬态录成“任务初始状态”，既不真实也会污染动作标签。

### 4.3 支架和货架偏移必须保持视觉、碰撞与标签一致

移动支架或货架时，必须同步更新：

- 可见 mesh；
- collision geom；
- 目标 site；
- 专家目标；
- 成功评价器；
- manifest 中的 applied transform。

只移动视觉不移动碰撞，或者只改专家目标不改成功判定，会产生无法通过增加数据修复的错误标签。

### 4.4 动力学随机化的边界

现有质量和摩擦档已经覆盖约±15%。第一轮优先补充它们与长度、位姿、支架高度的交互，不建议立刻继续放大范围。

如果需要覆盖更大的重量和摩擦变化，应先做二选一：

1. 验证低层夹爪在同一开合命令下可将接触力和滑移控制在可接受范围；
2. 确认真机有可用力/触觉，并设计新的观测契约。

否则外观相同的样本会要求模型在相同输入下输出不同力度，行为克隆只能学到折中值。

---

## 5. 数据准确性与自动验收

### 5.1 每条 episode 必须保存的元数据

除现有 manifest 字段外，v16 建议新增：

- `scenario_family`：H/T/R/C；
- `scenario_variant`：如 `single_hand_contact`；
- `scene_group_id`：用于防止近重复场景跨 split；
- `counterfactual_pair_id`：颜色命令成对场景；
- `start_phase`、`terminal_phase`；
- `intervention_type`、`intervention_frame`、`recovery_start_frame`；
- `target_object_id`、`target_color`、`distractor_object_ids`；
- 棒子、支架、桌子、货架和相机的 requested/applied transform；
- 质量、各类摩擦、夹爪控制参数；
- 首次双手抓持帧、首次稳定抬升帧、最大高度、滑移事件；
- 左右指垫、力、力比等仅用于验收的诊断量；
- 最终成功证据和各阶段 gate。

### 5.2 正向完整/阶段轨迹的建议门槛

| 项目 | 建议门槛 |
|---|---|
| 相机 | 3路、30 Hz、连续、RGB、同一分辨率契约 |
| 图像—状态 skew | ≤20 ms |
| 状态/动作 | 18维 float32、有限值、命名与顺序一致 |
| 严格抓持 | 左右均两指垫接触，双手均达到有效力 |
| 专家抓取力 | 首轮建议主要落在 15–26 N/手，左右比 ≤1.25；最终阈值由 pilot 分布确认 |
| 抬升 | ≥70 mm，且至少持续1秒 |
| 支架清除 | 棒子和手臂不再由拾取支架支撑 |
| 搬运 | 转向和移动后仍保持严格双手持物 |
| 插入 | 轴向、深度、前唇间隙满足几何 gate |
| 释放 | 两手完全无接触，棒子稳定在顶层 ≥2秒 |
| 静止帧 | 非必要静止不超过episode的10% |

力只用于数据质量 gate，除非真机也提供相同定义和带宽的传感器，否则不要悄悄加入策略输入。

### 5.3 恢复轨迹的额外门槛

- 构造的失败状态必须真实、可见、可恢复；
- 扰动期间不把无意义 action 当成专家动作；
- 接管后的前20步有明确纠错；
- 恢复完成后必须重新满足对应阶段 gate；
- 同一失败状态不能同时存在“继续抬升”和“放回重抓”两种未标明条件的冲突动作；
- 对每类恢复至少抽查首、中、末三帧和完整多视角视频；
- 专家执行失败时不进入训练集，但保留状态参数供下一次重试和评测。

### 5.4 多物体颜色轨迹的额外门槛

- prompt 中颜色与 `target_object_id` 一致；
- 目标棒最终成功；
- 干扰棒移动≤20 mm；
- 两条反事实 episode 的初始场景在容差内相同；
- 左右位置和颜色不能形成固定相关；
- prompt 模板不能与某一颜色、位置或难度绑定；
- split 按场景组，不按单 episode。

### 5.5 新版本契约不可省略

当前 `sorting_roll_validate.py` 强制要求完整任务最终放置、撤手和一分钟 gate；训练入口还硬编码300条和5个 task。阶段轨迹和多物体数据不能直接通过旧规则。

应新建 v16 契约，至少包含：

- 不同 `scenario_family` 的必需 gate；
- 动态 episode 数和 task 数；
- group-aware split；
- 多物体目标/干扰物元数据；
- 恢复扰动审计；
- 阶段轨迹的最短长度和50步未来动作检查；
- v15 旧数据的兼容导入与不可变审计。

---

## 6. 如何在少量数据下最大化信息量

### 6.1 不做全笛卡尔积，做风险驱动的条件覆盖

选择样本时按以下优先级：

1. 已真实观察到的失败状态；
2. 失败的相邻状态；
3. 两个高风险因素共同出现的交互项；
4. 运行域边界；
5. 仅影响外观、已被充分覆盖的低风险因素。

先固定低风险因素，再隔离验证一个核心能力。例如颜色 grounding 的第一轮让两根棒尺寸和动力学相同；否则抓对颜色和偏好更容易抓的物体会混在一起。

### 6.2 使用反事实配对

高信息样本应做到“尽量只改变一个会改变正确动作的变量”：

- 同一双棒场景，只改目标颜色；
- 同一失败姿态，一条从正确恢复动作开始，另一条改变可见失败量；
- 同一几何场景，交换左右位置；
- 同一棒子位姿，改变货架位置并让专家重新视觉对准。

这比随机生成两个完全不同场景更容易让模型学到因果条件。

### 6.3 增加关键帧比例，而不是复制同一轨迹

不能简单复制旧抓取片段。应采新的、物理不同的阶段 episode，并在训练采样中保证高价值族的比例。

建议首轮 batch 采样目标：

- 旧 v15 完整成功数据：约50%；
- H 困难完整轨迹：约15%；
- T 阶段聚焦轨迹：约15%；
- R 恢复轨迹：约15%；
- C 多物体语言轨迹：约5%。

如果当前训练器不支持按数据族加权，优先通过阶段短轨迹和显式 sampler 实现，不建议物理复制大量文件冒充新数据。最终比例必须从实际被采样的帧数核算，而不是只看 episode 数。

### 6.4 训练与采集要闭环迭代

不一次性采满160条。建议：

1. 16条 pilot；
2. 训练一个3k–5k step canary；
3. 在固定小型闭环套件上看阶段指标；
4. 只扩充被验证有效的数据族；
5. 每40条增量做一次 checkpoint canary；
6. 160条完成后再做正式微调和完整评测。

如果某类数据增加后 teacher-forced 动作改善、闭环仍不改善，应停止继续采同类数据，转查可观测性、低层控制或状态漂移。

---

## 7. 数据集拆分与评测设计

### 7.1 split 原则

建议总计 `128 train / 16 val / 16 test`，但必须 group-aware：

- 同一初始场景的颜色反事实对放在同一 split；
- 同一失败状态的不同扰动幅度放在同一 split；
- 同一基础场景派生的相机、光照版本不跨 split；
- val/test 保留未见过的因素组合，而不只是未见过的 seed；
- 旧 v15 的 val/test 不并入新训练集。

### 7.2 固定 checkpoint canary

不要再只看 seed3000。先建立12个固定场景的小门槛：

- 3个 nominal；
- 3个长棒×重低摩擦×边界；
- 3个支架/高度/货架偏移；
- 3个恢复状态。

每个 checkpoint 先跑一次固定 policy seed。满足以下条件才扩大到多采样种子和完整 test：

- 至少10/12到达可抓工作区；
- 至少9/12严格双手抓持；
- 至少8/12稳定抬升≥70 mm；
- 至少6/12端到端成功；
- 无连续旋转、明显碰落或安全 gate 失败。

正式评测再加入：

- 多个 policy sampling seed；
- 颜色双目标；
- hard 组合；
- 恢复扰动；
- 按阶段报告置信区间，而不是只给总成功率。

### 7.3 checkpoint 选择

选择顺序应为：

1. 安全和无碰落；
2. 严格双手抓持率；
3. 稳定抬升率；
4. 持物通过转向率；
5. 最终成功率；
6. 才参考训练 loss。

不能默认最后一个 checkpoint 最好，也不能只用一次最大抬升高度选模型。

---

## 8. 4卡/8卡4090时间估算

### 8.1 真实基线

现有 300 条 campaign 的实际记录：

- manifest 创建：22:08；
- 8卡初始采集完成：23:54；
- 初始采集墙钟约106.6分钟；
- 每卡平均约2.8分钟/完整episode；
- 7条替换并行完成约2.9分钟；
- 300条验证约50秒；
- v2.1构建、v3.0转换和审计约40分钟；
- manifest 到训练就绪总计约150分钟。

因此自动采集阶段可用公式：

```text
collection_minutes ≈ 2.8 × ceil(accepted_episodes / gpu_count)
```

再为专家失败、进程启动和 I/O 波动预留15%–25%。

### 8.2 推荐160条的估算

| 环节 | 8×4090 | 4×4090 | 说明 |
|---|---:|---:|---|
| 160条理想采集 | 约56分钟 | 约112分钟 | 按现有完整episode吞吐保守估计 |
| 加15%–25%重试/波动 | 约64–70分钟 | 约129–140分钟 | recovery/hard 数据可能比v15更易失败 |
| finalize、选择、源验证 | 约5–10分钟 | 约5–10分钟 | 主要是CPU和I/O，不随GPU线性缩短 |
| v2.1/v3.0构建与审计 | 约25–35分钟 | 约25–35分钟 | 按300条约40分钟线性缩放并留余量 |
| **自动流水线合计** | **约94–115分钟** | **约159–185分钟** | 代码已就绪的情况 |
| **建议排期窗口** | **2–2.5小时** | **3–4小时** | 包含排队、检查和小规模返工 |

阶段 episode 比完整 episode 短，实际采集可能更快；这里仍按现有完整轨迹吞吐估算，以免低估。多物体渲染和恢复控制也可能增加成本，二者大致抵消后，不应承诺低于表中时间。

### 8.3 一次性开发时间

| 工作 | 估算 |
|---|---:|
| v16 manifest、group split、元数据和验证器 | 0.5–1工程日 |
| 支架/高度/货架/相机一致变换与admission | 0.5工程日 |
| 7类扰动注入与自动恢复专家 | 0.5–1.5工程日 |
| 多棒场景、目标绑定、干扰物成功判定 | 0.5–1工程日 |
| **可并行部分合并后的现实总预算** | **约1.5–3工程日** |

如果自动恢复专家无法安全覆盖某一类，可只对那一类使用人工接管。单人手工录制56条恢复数据，按每条5–8分钟的操作、复位和初审估算，为4.7–7.5小时；加返工后建议预留5–8小时。4卡或8卡不会让一个操作员同时变成多个人。

---

## 9. 实施顺序与停止规则

### 阶段0：先验证根因，不采大批数据

对28k、36k、40k在专家 seed3000 成功轨迹的关键帧做 teacher-forced probe：

- 桌前观察；
- 预抓；
- 首次接触前；
- 双手刚闭合；
- 抬升开始；
- 离桌后。

比较前50步预测与专家动作，重点看：

- 底盘速度和角速度；
- 双手夹爪闭合时机；
- 左右臂同步误差；
- 抬升关节方向；
- chunk 前20/30/50步是否存在阶段差异。

解释：

- 专家观测下也不输出抬升：优先补T/H正向数据或检查训练；
- 专家观测下正确，一旦偏移几毫米就失败：优先补R恢复数据；
- 预测动作正确但执行力异常：优先检查低层夹爪控制、动作映射和不可观测力问题；
- 底盘在专家观测下仍过度旋转：优先补导航纠偏并检查base action统计。

### 阶段1：16条pilot

第一批只录：

- 4条长棒×重低摩擦×边界的干净抓取—抬升；
- 4条miss/棒子被推移后的重定位；
- 4条单手接触/部分滑落后的安全放回重抓；
- 4条支架高度或整套拾取机构偏移。

先不在pilot里做颜色，因为颜色必须先完成多物体场景和目标判定，不能用单棒伪验证。

pilot通过条件：

- 16条全部满足各自正向或恢复gate；
- 无错误策略前缀进入训练标签；
- 三相机、状态、动作和元数据审计通过；
- 3k–5k step canary 后，固定12场景中抓持/抬升指标相对原始36k有明确提升，且导航没有退化。

### 阶段2：扩到80条

若pilot有效，再补到：H20、T20、R28、C12。完成一次中期微调和闭环评测。

停止规则：某数据族增加20条后，其对应阶段指标没有改善，就暂停该族并重新检查观测可辨识性、标签、低层控制或采样权重。

### 阶段3：扩到160条

只有中期评测表明至少一个核心阶段有可复现提升，才完成全部160条。之后用固定 canary 和完整 held-out test 选 checkpoint，不用训练 loss 选最终模型。

---

## 10. 风险与成立前提

这条路径成立需要以下前提：

1. 三相机视角能在各关键阶段看见足够的棒子、双手和货架信息；
2. 所有新增随机量都在真实部署可能出现且专家可达的范围内；
3. 低层夹爪能在没有策略力反馈时提供基本稳定、限幅的接触；
4. 如果加入力/触觉，真机必须提供同定义、同频率、同延迟的信号；
5. 恢复片段只保留正确接管动作，不把坏动作当正标签；
6. v16验证器、构建器和训练入口不再硬编码旧300条/5任务契约；
7. 多物体成功判定按目标ID，而不是“任意一根棒子进槽”；
8. split按场景组隔离，避免颜色反事实对和同源扰动泄漏；
9. 训练采样能让T/R数据获得足够帧权重；
10. checkpoint使用多场景闭环指标选择，而不是默认训练越久越好。

主要风险：

- 恢复专家不自然，模型学会脚本痕迹而不是真正纠错；
- 中间状态起始分布与真实闭环失败状态不一致；
- 随机化范围过大导致专家只在使用仿真真值时成功，视觉策略无法复现；
- 多物体新增后遮挡和碰撞改变任务难度，24条不足以同时学习颜色和复杂几何；
- 无力反馈情况下继续扩大动力学范围，产生相同输入对应冲突动作；
- 新数据过权重导致旧导航或放置能力遗忘。

---

## 11. 最终要求的四项输出

### 11.1 原方案中只在修补表面的部分

1. 继续从28k训练到31k、36k、40k，希望步数自然解决闭环问题；
2. 反复调整 `REPLAN=10/20/30/50`，只改变失败发生的位置；
3. 继续均匀增加颜色、光照、JPEG等边际变化，却不补关键联合条件；
4. 每次专家失败都用成功episode替换，保持“全成功”但删除恢复状态；
5. 用单棒不同颜色和泛化同义句，误以为已经训练颜色语言；
6. 再加一批完整50秒轨迹，让大量导航帧继续淹没抓取/抬升监督；
7. 只看总成功或最大抬升，不看导航、接触、承重、持物、插入各阶段转化率。

### 11.2 从基本事实重新推导出的新路径

1. 先用teacher-forced关键帧诊断区分“没学会动作”和“闭环偏移后不会恢复”；
2. 明确定义运行域和可观测性，不采策略无法区分的冲突条件；
3. 用40条困难完整轨迹补关键交互格；
4. 用40条阶段轨迹把有效抓取/抬升监督提高，而不是增加导航冗余；
5. 用56条从真实失败状态开始的正确恢复轨迹补闭环缺口；
6. 用24条双棒成对反事实轨迹建立真正的颜色语言grounding；
7. 新建v16元数据、group split和按数据族验收；
8. 每16/80/160条分阶段训练和评测，无收益的数据族立即停止；
9. 对不可观测的力度问题使用低层控制或真实传感器，不把它错误归结为“数据不够”；
10. 用多场景阶段指标选择checkpoint。

### 11.3 这条路径成立的前提

最关键的前提是：新增数据必须让当前观测中的可辨识变化与正确动作形成稳定关系。视觉可见的几何偏移、目标颜色和失败姿态可以通过数据学习；不可见的接触力、质量和摩擦不能仅靠更多RGB示范可靠解决。恢复示范必须是失败状态后的正确动作，不能包含错误动作标签。所有变换、碰撞、目标和评价器必须保持物理一致。

### 11.4 验证它的第一步

**第一步不是立刻采160条，而是完成28k/36k/40k在专家轨迹关键帧上的teacher-forced动作对照。**

这一检查成本最低，却能决定第一批16条pilot应该偏向：

- 正确抓取—抬升正样本；
- 闭环失败恢复样本；
- 低层夹爪/动作契约修复；
- 导航纠偏样本。

得到该结论后，再实施16条pilot，并以固定12场景闭环canary验证。只有pilot带来可复现阶段提升，才投入4卡或8卡完成后续144条。
