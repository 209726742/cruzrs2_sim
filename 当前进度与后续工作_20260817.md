# CRUZR 双物料线当前进度与后续工作

> 状态时间：2026-08-17 12:50 UTC（北京时间 20:50）  
> 当前主线：使用现有 50 条、候选 canary 20 条和正式主采 230 条，共 300 条源 episode，构建统一 LeRobot v3.0 数据集并开展 4 卡 π0.5 训练。  
> 重要口径：第三人称 `preview_3rd.mp4` 只用于人工审阅和诊断，不进入 π0.5 训练输入。

## 一、当前结论

原始数据采集和本轮第三人称视频后处理已经完成，但完整 300-source LeRobot 数据集尚未构建，因此当前可以进入“统一复验、数据集构建和训练预检”阶段，不能跳过这些步骤直接启动全量 π0.5 训练。

当前推荐路线是：冻结 300 条源数据 → 构建 v2.1 → 转换并审计 v3.0 → 4 卡 20-step DDP canary → 通过后开始正式训练 → 在 step 10k 做第一轮闭环效果决策。

## 二、已完成工作

### 2.1 原始源数据

| 数据批次 | 数量 | 模式配比（normal / boundary / recovery） | 状态 |
|---|---:|---:|---|
| 既有 pilot | 50 | 35 / 10 / 5 | 已严格复验，50/50 PASS |
| 候选 canary | 20 | 14 / 4 / 2 | manifest=`complete`，20/20 PASS |
| 正式主采 | 230 | 161 / 46 / 23 | manifest=`complete`，230/230 accepted |
| 合计 | 300 | 210 / 60 / 30 | 原始 episode 已齐，符合 70% / 20% / 10% 配比 |

正式主采信息：

- campaign：`candidate_formal_main_v24_4g_20260816`
- 采集完成时间：2026-08-17 09:33 UTC
- accepted：230/230
- rejected：182；失败尝试保留在隔离目录，不进入发布数据
- 四个 normal、boundary、recovery 分片均以 return code 0 完成
- 权威状态文件：`log/candidate_formal_main_v24_4g_20260816_runtime/candidate_formal_main_v24_4g_20260816_multigpu_manifest.json`
- 发布数据目录：`cruzr_mujoco_sim/out/teleop/candidate_formal_main_v24_4g_20260816`

采集器在发布 episode 前执行单条源验收；但三批数据合并后的最终 300-source 冻结审计尚未执行，后续仍要统一复验一次。

### 2.2 第三人称视频

- 正式主采第三人称视频：230/230 个 `preview_3rd.mp4`
- 正式主采后处理完成时间：2026-08-17 12:41 UTC
- 后处理退出码：`rc=0`
- 当前无第三人称渲染进程
- 候选 canary 第三人称视频此前已完成 20/20

第三人称视频由 `shelf_e2e_replay_3rd.py` 从源状态重放生成，不是策略相机输入。π0.5 仍只使用以下三路相机：

- `stereo_left`
- `waist_front`
- `chassis_front`

### 2.3 已有数据集与训练基线

目前已存在且通过审计的是旧 50-source 数据集：

- v2.1：`cruzr_mujoco_sim/out/datasets/pilot_balanced_v24_50source_lerobot_v21_20260816`
- v3.0：`cruzr_mujoco_sim/out/datasets/pilot_balanced_v24_50source_lerobot_v30_20260816`
- 50 个唯一源 episode
- 876 个派生 episode
- 689,460 frames
- state/action 均为 18 维
- 三路策略相机、source-level split、q01/q99 和抽样实际解码已通过

现有 π0.5 训练基线：

- step 500 checkpoint 已完成并可复载
- step 1000 checkpoint 已完成并可复载
- checkpoint：`cruzr_mujoco_sim/out/training/pilot_train_v24_50source_500step_20260816/checkpoints/001000`
- 训练过程无 NaN、Inf、OOM 或保存错误
- 未见 seed 6 的 step-500 和 step-1000 闭环均为 0 grasp / 0 placed，当前模型不可用于任务

### 2.4 机器资源

- GPU：4× NVIDIA GeForce RTX 4090，当前均空闲
- 单卡显存：24,564 MiB
- 工作区存储余量约 6.2 PiB，inode 余量充足
- 训练环境已有 Accelerate 1.14.0，4 卡 DDP 代码路径已核验

## 三、尚未完成与当前阻塞

### 3.1 300-source 数据集尚未构建

当前磁盘上只有 50-source LeRobot v2.1/v3.0 数据集。新增的 20 + 230 条仍是原始源 episode，尚未完成：

- 统一 300-source 输入 manifest
- 300 条最终冻结契约复验
- 300-source LeRobot v2.1 构建
- v2.1 → v3.0 离线转换
- source-level train/val/test 隔离审计
- 三路视频、18D state/action、统计量和实际解码检查

### 3.2 不能直接把旧 step-1000 切换到新数据后 resume

现有 `001000/pretrained_model/train_config.json` 绑定旧 50-source 数据集。训练入口在 `resume=true` 时会恢复 checkpoint 自带的 processor，不会自动使用新 300-source 数据集统计重建归一化处理器。

因此不建议简单修改 dataset root 后直接 resume。推荐以 300-source v3.0 的统计从基础 π0.5 开一个新的干净训练；旧 step-1000 保留为对照。如果后续要做 step-1000 微调实验，应单独设计“新数据统计 + 权重迁移 + 优化器是否重置”的受控方案，不能与正式基线混为一条实验。

### 3.3 数据仍属于受限候选口径

正式采集前的代表 sweep 为 16/26，低于原定 24/26 准入门。本轮按用户授权使用 candidate readiness override 启动，并接受场景覆盖偏差；每个发布 episode 仍保持 100% 单条源验收。

此外，条料物理参数仍基于工程假设，不代表真实材料标定。因此后续模型结果应标记为仿真候选结果，不应直接宣称可安全部署到真机。

## 四、后续执行计划与验收标准

### 阶段 A：冻结统一 300-source 清单

工作项：

1. 新建独立的 300-source manifest 目录，仅使用符号链接，不复制原始大文件。
2. 合并既有 50、候选 20 和主采 230 条发布 episode。
3. 检查 300 个链接全部可解析、无重复路径、无重复 seed。
4. 检查模式配比为 210 normal / 60 boundary / 30 recovery。
5. 使用最终冻结版本的 `shelf_e2e_source.py` 对 300 条逐条复验。

通过条件：

- found=300，accepted=300
- unique seeds=300
- validator `passed=true`、`errors=[]`
- `.rejected/` 和失败尝试不进入 manifest
- collection profile 与 task version 全部一致

若复验出现失败，只隔离失败源并记录原因，不通过复制、改元数据或降低门槛强行补入训练集。

### 阶段 B：构建 LeRobot v2.1

工作项：

1. 使用全新的 OUT 目录，禁止覆盖现有 50-source 数据集。
2. `EPISODES` 只指向阶段 A 冻结的 300-source manifest。
3. 使用已验证的 `shelf_e2e_build_v2.py` 构建。
4. 建议保持 `ENCODE_WORKERS=3`，并在正式构建前做少量源的编码 smoke。

通过条件：

- 300 个源全部被构建器识别并接受
- `rejected_sources.jsonl` 为空
- 所有派生 episode 均保持 source seed 和 source split
- 任一 source 的派生 clips 不跨 train/val/test
- 三路策略相机视频均通过帧数、FPS、PTS 和可解码检查
- state/action 维度均为 18

### 阶段 C：转换并审计 LeRobot v3.0

工作项：

1. 保留 v2.1 原件，复制到全新 v3.0 目标后离线转换。
2. 设置 `push_to_hub=false`，不依赖网络。
3. 独立检查 Parquet 行数、episode 数、视频分块和元数据。
4. 对 train、val、test 各抽样实载至少一个 episode。

通过条件：

- `total_source_episodes=300`
- source-level split 目标约为 240 / 30 / 30，且严格无源级泄漏
- `observation.state` 与 `action` 的 min/max/mean/std/q01/q99 均为有限 18 维值
- 三路图像实载为预期 shape，state/action 实载为 `(18,)`
- 不出现损坏视频、空 Parquet、缺失 episode 或重复 source

### 阶段 D：4 卡 20-step DDP canary

工作项：

1. 使用新的 300-source v3.0 train split。
2. 从基础 π0.5 建立新训练输出目录和新 processor 统计。
3. 使用 4 个进程、GPU 0–3、batch size 1/GPU、num_workers 2/GPU。
4. 只运行 20 steps，并保存 canary checkpoint。
5. 记录每步耗时、数据等待、显存峰值和 loss/grad/lr。

通过条件：

- 四个 rank 均正常启动和退出
- 无 OOM、NaN、Inf、Traceback 或 DataLoader 错误
- checkpoint 可完整复载
- loss、grad、lr 全部有限
- 三路图像、state18/action18 和语言输入 shape 正确

### 阶段 E：正式训练与闭环评测

canary 通过后再启动正式训练。第一阶段建议以 step 10k 为效果检查点，不把 loss 下降当作任务成功。

评测顺序：

1. 用未见 seed 6 做固定条件闭环复测。
2. 首先观察是否出现真实 grasp，而不是只看物料位移或 loss。
3. 若 seed 6 出现稳定 grasp，再扩到至少 5 个未见 seeds。
4. 记录 grasped、placed、success、碰撞/力、轨迹和视频。
5. 若 step 10k 仍为 0 grasp，暂停盲目长训，优先排查长时序动作学习、导航样本比例、动作 chunk 与演示分布。

阶段决策：

- 有稳定 grasp/placed 信号：继续训练并扩大未见 seed 评测。
- loss 下降但仍 0 grasp：不直接追加大量 steps，先诊断数据和时序学习问题。
- 出现 NaN/OOM/数据错误：回到 canary 修复，不启动长训练。

## 五、推荐的立即下一步

当前最优下一步是执行阶段 A：创建统一 300-source manifest 并完成最终逐条复验。阶段 A 通过后再启动耗时较长的数据集构建。

在 300-source v3.0 审计通过前：

- 不启动全量 π0.5 训练
- 不覆盖现有 50-source 数据集或 step-1000 checkpoint
- 不把 `preview_3rd.mp4` 加入训练 schema
- 不把 `.rejected/` 中的失败尝试加入训练集
- 不因追求总数 300 而降低单条源验收门槛

## 六、关键文件与目录

| 用途 | 路径 |
|---|---|
| 正式主采 manifest | `log/candidate_formal_main_v24_4g_20260816_runtime/candidate_formal_main_v24_4g_20260816_multigpu_manifest.json` |
| 正式主采发布数据 | `cruzr_mujoco_sim/out/teleop/candidate_formal_main_v24_4g_20260816` |
| 候选 20 条发布数据 | `cruzr_mujoco_sim/out/teleop/candidate_canary_v24_4g_20260816` |
| 既有 50 条 manifest | `cruzr_mujoco_sim/out/manifests/pilot50_sources_v24_20260816` |
| 50-source v3.0 | `cruzr_mujoco_sim/out/datasets/pilot_balanced_v24_50source_lerobot_v30_20260816` |
| step-1000 对照 checkpoint | `cruzr_mujoco_sim/out/training/pilot_train_v24_50source_500step_20260816/checkpoints/001000` |
| 单条源验收器 | `cruzr_mujoco_sim/scripts/collection/shelf_e2e_source.py` |
| v2.1 构建器 | `cruzr_mujoco_sim/scripts/collection/shelf_e2e_build_v2.py` |
| v2.1→v3.0 转换器 | `src/lerobot/datasets/v30/convert_dataset_v21_to_v30.py` |
| π0.5 训练入口 | `src/lerobot/scripts/lerobot_train.py` |
| 第三人称完成日志 | `log/candidate_formal_main_v24_4g_20260816_thirdperson.log` |

## 七、完成定义

本轮“数据到训练”工作只有同时满足以下条件才算完成：

- 300 条源数据通过统一冻结审计
- 300-source v2.1 和 v3.0 均构建完成且可回滚
- source-level split、统计、三路视频和实际解码通过
- 4 卡 DDP canary 通过并保存可复载 checkpoint
- 正式训练至少到达预定评测 checkpoint
- 未见 seed 闭环结果已记录，且用 grasp/placed/success 判断效果
- 所有命令、日志、配置、数据集路径和决策均更新到进度文档
